import React from "react";
import { observer } from "mobx-react-lite";

export const Complex = observer(() => {
  const { count, isPositive, onAdd, onSubtract } = {};

  return <>Collection or some shit</>;
});
